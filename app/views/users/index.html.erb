<% template = parse_liquid_template("app/views/users/daily_records.liquid") %>

<%= template.render({ 'daily_records' => @daily_records }).html_safe %>

<% users_template = parse_liquid_template("app/views/users/users.liquid") %>

<%= users_template.render({ 'users' => @users }).html_safe %>
